name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SECRET_KEY: ci-dummy-secret-key
      DEBUG: "False"
      SECURE_SSL_REDIRECT: "False"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Django system check (deploy)
        run: python manage.py check --deploy

      - name: Collect static files
        # WhiteNoise uses hashed names (CompressedManifestStaticFilesStorage),
        # so templates need the manifest that collectstatic generates.
        # staticfiles/ is git-ignored; production builds it on boot (deploy.sh).
        run: python manage.py collectstatic --noinput

      - name: Test suite
        run: python manage.py test
